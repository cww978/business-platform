<view>
  <message
    title="{{title}}"
    type="{{type}}" 
    mainButton="{{messageButton.mainButton}}" 
    subButton="{{messageButton.subButton}}" 
    onTapMain="goHome"
    onTapSub="goBack">
  </message>
</view>