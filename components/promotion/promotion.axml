<view class="component-promotion">
  <popup zIndex="2" show="{{show}}" animation="{{true}}" position="bottom">
    <view class="picker-title">
      <view class="padding" onTap="clickCancel"><text class="text-blue">取消</text></view>
      <view class="padding" onTap="clickItem"><text class="text-blue text-bold">确认</text></view>
    </view>
    <picker-view value="{{value}}" onChange="onChange" class="my-picker">
      <picker-view-column a:if="{{type == 1}}">
        <block a:for="{{promotionProducts}}">
          <view>{{item.text}}</view>
        </block>
      </picker-view-column>
      <picker-view-column a:else>
        <block a:for="{{promotionTobaccos}}">
          <view>{{item.text}}</view>
        </block>
      </picker-view-column>
      <picker-view-column a:if="{{num}}">
        <block a:for="{{99}}">
          <view>{{item}}</view>
        </block>
      </picker-view-column>
      <picker-view-column a:if="{{unit}}">
        <block a:for="{{units}}">
          <view>{{item.text}}</view>
        </block>
      </picker-view-column>
    </picker-view>
  </popup>
</view> 